<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProyectoBanco" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/Top</item>
        <item name="android:statusBarColor">@color/Top</item>
    </style>

    <style name="Splash" parent="Base.Theme.ProyectoBanco">
        <item name="android:windowBackground">@drawable/bg_splash</item>
    </style>
    
    <style name="Theme.ProyectoBanco" parent="Base.Theme.ProyectoBanco" />
</resources>